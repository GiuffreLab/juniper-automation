---
- name: Compare Juniper SRX Rollbacks
  hosts: all
  connection: ansible.netcommon.netconf
  gather_facts: no
  roles:
    - juniper.junos
  tasks:
    - name: Running show system rollback command
      juniper_junos_command:
        commands:
          - show system rollback
      register: rollbacks
